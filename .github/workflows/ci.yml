name: CI - Lint, Test, Build and Push Image (ui)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    name: Run ESLint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

  test:
    name: Run UI tests
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  build:
    name: Build and Push Docker image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry (with PAT)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: wilsonn06
          password: ${{ secrets.GHCR_PAT }}

      - name: Build Docker image
        run: |
          OWNER="wilsonn06"
          SHA_SHORT="${GITHUB_SHA::7}"
          IMAGE_NAME="ghcr.io/$OWNER/s-rusun-ui:sha-$SHA_SHORT"
          echo "IMAGE_NAME=$IMAGE_NAME" >> "$GITHUB_ENV"
          docker build -t "$IMAGE_NAME" .

      - name: Push Docker image
        run: docker push "$IMAGE_NAME"

      - name: Notify infra repo to deploy
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.REPO_PAT }}
          repository: wilsonn06/s-rusun-infra
          event-type: image-update
          client-payload: |
            {
              "service": "s-rusun-ui",
              "image": "${{ env.IMAGE_NAME }}"
            }